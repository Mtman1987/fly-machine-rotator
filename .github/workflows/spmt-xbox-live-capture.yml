name: SPMT Xbox live capture

on:
  push:
    branches:
      - ops/spmt-xbox-live-capture
    paths:
      - ".github/workflows/spmt-xbox-live-capture.yml"

permissions:
  contents: write

jobs:
  capture:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ops/spmt-xbox-live-capture
          fetch-depth: 0
      - uses: superfly/flyctl-actions/setup-flyctl@v1
      - name: Capture current SPMT status and logs
        run: |
          set -euo pipefail
          mkdir -p capture
          flyctl status -a spmt-live --json --access-token "$FLY_API_TOKEN" > capture/status.json
          flyctl logs -a spmt-live --json --no-tail --access-token "$FLY_API_TOKEN" > capture/logs.jsonl || true
          flyctl machine list -a spmt-live --json --access-token "$FLY_API_TOKEN" > capture/machines.json
      - name: Upload capture
        uses: actions/upload-artifact@v4
        with:
          name: spmt-xbox-live-capture
          path: capture/
          retention-days: 1
      - name: Remove one-shot workflow
        run: |
          rm .github/workflows/spmt-xbox-live-capture.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Remove temporary SPMT Xbox capture workflow"
          git push origin HEAD:ops/spmt-xbox-live-capture
