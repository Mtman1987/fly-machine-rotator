import { readFile, writeFile } from 'node:fs/promises';

const path = 'src/publicCodexFixer.ts';
let source = await readFile(path, 'utf8');
if (source.includes('ATHENA_ROOM_CONTROL_V1')) {
  console.log('private Athena Coder control already patched');
  process.exit(0);
}

const handlerMarker = 'export async function handlePublicCodexRequest(request: IncomingMessage, response: ServerResponse, env: NodeJS.ProcessEnv): Promise<boolean> {';
if (!source.includes(handlerMarker)) throw new Error('Athena Coder handler marker missing');

const helpers = `// ATHENA_ROOM_CONTROL_V1\ntype AthenaRoomControlInput = CreateJobInput & {\n  action?: string;\n  jobId?: string;\n  artifact?: string;\n  confirmed?: boolean;\n};\n\nasync function createAthenaRoomJob(input: AthenaRoomControlInput, identity: Record<string, unknown>, env: NodeJS.ProcessEnv) {\n  if (!String(env.SPMT_LLM_BASE_URL || env.OPENAI_API_KEY || '').trim()) {\n    throw new Error('No Athena Coder model provider is configured');\n  }\n  const description = String(input.description || '').trim();\n  if (!description) throw new Error('description is required');\n  const repo = inferRepo(input);\n  const now = new Date().toISOString();\n  const job: PublicCodexJob = {\n    id: \`mtfix_\${Date.now()}_\${randomUUID().slice(0, 8)}\`,\n    status: 'queued',\n    createdAt: now,\n    updatedAt: now,\n    source: String(input.source || 'hearmeout-private-athena'),\n    reporter: String(identity.username || input.reporter || 'SPMT owner'),\n    reporterId: String(identity.id || input.reporterId || ''),\n    tenantId: input.tenantId,\n    appName: String(input.appName || repo.appNames[0]),\n    repoId: repo.id,\n    description: description.slice(0, 4000),\n    changedFiles: [],\n    checks: [],\n  };\n  await saveJob(env, job);\n  void executeJob(job, input, repo, env);\n  return job;\n}\n\nasync function readAthenaRoomArtifact(env: NodeJS.ProcessEnv, jobId: string, artifact: string) {\n  const id = safeJobId(jobId);\n  if (!id) throw new Error('Invalid job id');\n  const kind = artifact === 'diff' ? 'diff.patch' : artifact === 'checks' ? 'checks.txt' : artifact === 'response' ? 'response.txt' : '';\n  if (!kind) throw new Error('artifact must be diff, checks, or response');\n  return readFile(join(rootDir(env), 'jobs', id, kind), 'utf8');\n}\n\nasync function handleAthenaRoomControl(input: AthenaRoomControlInput, identity: Record<string, unknown>, env: NodeJS.ProcessEnv) {\n  const action = String(input.action || 'status').trim().toLowerCase();\n  if (action === 'create') {\n    const job = await createAthenaRoomJob(input, identity, env);\n    return { ok: true, action, job, message: \`Athena Coder job \${job.id} queued for \${job.appName}.\` };\n  }\n  if (action === 'list') {\n    const jobs = await listCodexJobs(env, 20);\n    return { ok: true, action, jobs };\n  }\n  const jobId = String(input.jobId || '').trim();\n  if (!jobId) throw new Error('jobId is required');\n  const job = await readCodexJob(env, jobId);\n  if (!job) throw new Error('Job not found');\n  if (action === 'status') return { ok: true, action, job };\n  if (action === 'artifact') {\n    const artifact = String(input.artifact || 'diff').toLowerCase();\n    return { ok: true, action, jobId, artifact, content: await readAthenaRoomArtifact(env, jobId, artifact) };\n  }\n  if (action === 'publish') {\n    if (input.confirmed !== true) {\n      return {\n        ok: true,\n        action: 'confirmation-required',\n        confirmation: { type: 'publish-draft-pr', jobId, expiresInSeconds: 300 },\n        message: \`Confirm publishing validated job \${jobId} as a draft pull request. This will not merge or deploy it.\`,\n        job,\n      };\n    }\n    return { ok: true, action, job, pullRequest: await publishJob(job, env) };\n  }\n  throw new Error('Unsupported Athena Coder control action');\n}\n\n`;
source = source.replace(handlerMarker, helpers + handlerMarker);

const routeMarker = `  if (method === "GET" && url.pathname === "/athena/coder") {`;
if (!source.includes(routeMarker)) throw new Error('Athena Coder route marker missing');
const route = `  if (method === "POST" && url.pathname === "/api/athena/control") {\n    const identity = await requireSpmtAdmin(request, env);\n    if (!identity) return sendJson(response, 401, { error: "SPMT owner/admin authorization required" }), true;\n    try {\n      const input = await readJson(request) as AthenaRoomControlInput;\n      return sendJson(response, 200, await handleAthenaRoomControl(input, identity, env)), true;\n    } catch (error) {\n      const message = error instanceof Error ? error.message : String(error);\n      const status = /not found/i.test(message) ? 404 : /required|invalid|unsupported|artifact/i.test(message) ? 400 : /Only completed|Manual publication|GitHub PR/i.test(message) ? 409 : 502;\n      return sendJson(response, status, { error: message }), true;\n    }\n  }\n\n`;
source = source.replace(routeMarker, route + routeMarker);

await writeFile(path, source, 'utf8');
console.log('patched private Athena room Coder control');
